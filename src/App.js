import React from "react";
import { Router } from "@routes/Router";

const App = () => {
    return (
        <React.StrictMode>
            <Router />
        </React.StrictMode>
    )
}

export default App